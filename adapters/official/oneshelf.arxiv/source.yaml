# arXiv's API host answers on http; the PDF host is https.
base_url: http://export.arxiv.org
url_patterns:
  - pattern: '^https?://arxiv\.org/abs/([0-9]{4}\.[0-9]{4,5}(?:v[0-9]+)?)'
    capability: work
    id_group: 1
rate_limit:
  requests_per_minute: 20
  concurrency: 1
