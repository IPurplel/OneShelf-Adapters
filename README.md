# OneShelf Source Registry (generated)

This branch holds only generated artifacts: `index.json` (`oneshelf.registry/1`) and `packages/*.osp`.
It is produced by `./tools/publish-registry` from the adapter sources on `main` and read by OneShelf from
`https://raw.githubusercontent.com/IPurplel/OneShelf-Adapters/registry/index.json`.

Do not edit it by hand, and never force-push it.
