# PUBLIC Ed25519 keys whose signatures the Registry build and verification accept, one per line:
#   key-id:base64-public-key
# List two while rotating. Never put a private key here — or anywhere in this repository.
# Optional: signing is not required for this first-party Registry (docs/publishing.md). Empty for now.
