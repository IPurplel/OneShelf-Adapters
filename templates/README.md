# Templates

`basic-source/` is what `./tools/new-adapter` copies. It is a small, working adapter (one search recipe, a
synthetic fixture and a packaged test) so a new contributor starts from something that passes the checks.
Placeholders `{{ID}}`, `{{NAME}}`, `{{DOMAIN}}` and `{{PUBLISHER}}` are filled in by the tool.
